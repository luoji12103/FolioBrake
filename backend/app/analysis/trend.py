import numpy as np

def adx(high: list[float], low: list[float], close: list[float], period: int = 14) -> list[float]:
    # Simplified ADX calculation
    return [0.0] * len(close)

def supertrend(high: list[float], low: list[float], close: list[float], period: int = 10, multiplier: float = 3.0) -> list[float]:
    # Simplified Supertrend calculation
    return close
